# Embedded Trainer 使用说明

Embedded Trainer 用于在 VS Code 内学习和评测 C、C++、嵌入式、智能车、电子设计竞赛、Linux 和 ROS 2 基础课程。正常训练不需要手动打开终端或输入命令。

## 界面组成

### 0.12.0 离线检查与补齐环境

适用于 Windows 10/11 x64。先解压带教提供的整体离线包，安装 `plugin` 中的 VSIX，并打开自己的练习文件夹。按 Ctrl+Shift+P 选择“Embedded Trainer: 检查训练环境”；缺失时点击“从离线包自动安装”，选择整体包根目录或 `environment` 文件夹，再确认“确认并按默认路径安装”。安装期间不要关闭 VS Code，完成后自动复检。

Python 安装到 `%LOCALAPPDATA%\Programs\Python\Python313`；LLVM-MinGW 编译器和 Boost 头文件分别安装到 `%LOCALAPPDATA%\Programs\EmbeddedTrainer` 下的版本目录。安装无需管理员权限，不修改系统 PATH，也不会替换已检测可用的环境。请预留至少 6 GB 空间。插件自动记住路径，日常练习无需打开终端。配套环境仅用于电脑端 C11/C++17 练习，不替代单片机交叉编译工具链。

Boost 是部分 C++ 拓展题的可选依赖，只有它缺失时可以点“暂不安装”继续基础训练。配套文件由插件内固定 SHA-256 校验，不支持任意同名安装器。校验失败时重新获取完整可信包；默认目录已有不完整安装时不会强行覆盖，请把 Embedded Trainer 输出和默认目录内 `install-…` 日志交给带教处理，不要删除现有开发环境。

环境安装授权不等于题库代码信任。之后仍需在“管理知识包”中导入题库、核对来源并按版本授权编程题。整个流程无需访问 GitHub；在线更新才需要网络。VS Code 本体需事先安装，不包含在配套包中。

### 0.11.0 新生训练营路线图

侧边栏首项“新生训练营 · 路线图”是推荐入口。按能力阶段学习，不按周次，也没有强制解锁：先读概念和示例、预测结果，再完成必做题，最后记录阶段自查。“继续学习”会优先返回上次从路线打开但未通过的必做题。

主线为 7 个阶段、49 道必做题，选做题不影响完成。“完整题库”折叠分组仍可访问全部学科课程；缺失题目会显示未安装，可通过“管理知识包”补齐，不会被算作通过。

题目中的“先看思路提示”按次逐层展开，原题提示保留，未提交的答案草稿不会因提示刷新丢失。课程顶部“训练路线”可返回路线图。

“保存自查记录”只写入本机 `.trainer/camp-progress.json`，旧题目成绩仍在 `.trainer/progress.json`。请先写下解释或测试结果，再勾选自查完成；这不等于带教验收或已掌握。

“导出学习报告”可保存 Markdown 或 JSON，包含成绩摘要、自查文字与待复习题目明细，不自动收集源代码或上传到网络。分享前检查自己填写的记录。最后一个阶段配有[模拟智能车项目指南](camp-project.md)，由带教同学人工检查项目和讲解，不冒充自动评分。

带教同学可点击“导出路线模板”，保存为目标项目根目录的 `trainer-roadmap.json` 后编辑阶段和题目 ID。文件变化会刷新路线，无须重建插件；修改教学目标时递增路线版本，旧文字保留但新版自查需要重新确认。

- **活动栏 Embedded Trainer 图标**：打开课程路线。
- **新生训练营**：首项打开路线图；“完整题库”下按学习方向和题型分组。
- **课程详情面板**：阅读题目、提交答案、打开代码、运行评测和查看分级提示。
- **测试资源管理器**：运行单道或全部编程题。
- **问题面板**：显示编译错误和警告，点击可跳转到源码。
- **状态栏 Trainer 进度**：显示“已通过题数/题目总数”。
- **Embedded Trainer 输出**：保留完整后台评测日志，仅在排查问题时查看。

### 0.7.0 原生风格工作台

- 保留 VS Code 原生课程树，每条路线和题型显示题目数、已通过数；刷新时保留分组展开状态。
- 课程面板采用紧凑布局，题目、作答与评测结果在主区域，学习状态、知识点、前置课程与知识包版本在右侧。分屏较窄时，信息区自动移到下方。
- 顶部常驻“知识包”和“使用说明”，编程题的主要按钮会从“开始练习”切换为“运行评测”。
- 细线、冷色强调和等宽编号提供轻微的科幻感；背景、按钮和文字跟随 VS Code 主题，兼容浅色、深色和高对比度，没有闪烁或循环动画。
- 同一面板中查看提示或刷新课程，会保留当前题目尚未提交的答案。切换题目或关闭面板后不保证保留草稿；正式学习进度仍由训练核心保存。
- 可用 Tab 在按钮和输入框间移动，用方向键切换单选项；提交中暂时禁用控件，避免重复请求。

## 0.9.0 一键获取 + 集中审核

课程视图“…”或“管理知识包”里使用以下两个入口，普通学习者仍然只需要“更新题库”。

1. **获取新题**：自动接管此前的 Exercism 草稿，跳过已有题目/同源母题，优先入门、辅助进阶，每次最多尝试新增 20 题。逐题保存，失败不会丢失成功题。首批 32 道旧草稿无需重新挑选。联网扫描失败自动使用完整旧目录；未缓存的原文件仍需网络。
2. **集中审核**：统一显示等待验证、验证中、待人工审核、失败、已暂缓、内容已修改和已人工审核。浏览题目可查看题面、初始代码、参考代码、原始测试、来源和许可证；失败原因直接显示在列表。

首次云端配置只做一次：维护者先把 0.9.0 工具和 `.github/workflows/acquire-review.yml` 推送到目标仓库 main，然后允许云端验证并通过 VS Code 的 GitHub 登录授权。目标仓库在用户设置 `embeddedTrainer.authoringRepository` 中配置，工作区不能偷偷更换。没有工作流或授权时，草稿保留，不必重新获取；配置好后点“重新验证待处理题目”。插件不自动提交/推送代码，也不要求复制令牌。

GitHub 会按固定提交重建未编辑题目，并逐题在 Docker 验证。只发送题目标识、上游提交、内容校验值；本地编辑过的题会附带限定草稿路径，不发送源码或个人答案。编辑过的草稿必须先显式提交到仓库 main，插件再按相同路径和指纹验证。因 `knowledge/drafts/` 默认被忽略，维护者需有意识地仅纳入选定草稿，切勿公开 `.trainer/` 或 `exercises/`。

VS Code 打开且有待跟踪任务时，每 15 秒检查一次；网络异常按 30/60/120/240/300 秒退避，并尊重 GitHub 限流等待时间。没有任务或关闭插件后停止计时器，重开恢复；等待状态不反复通知。每个任务最多 50 题，更多待办可再点“重新验证”。网络错误保留原状态；任务匹配、SHA-256、题目指纹或证据不符时拒绝批准。

0.10.1 的云端工具会跨运行缓存固定上游提交的源码，缓存读取仍校验哈希，不缓存“通过”结论。GitHub Actions 运行详情的 Summary 会列出镜像准备、逐题重建和测试耗时。新增 GCC 14 + Boost 预制镜像：维护者推送工具后等待“Build validation image”完成，再在 GitHub Packages 将该镜像设为 Public（仅需一次）；在此之前云端使用同配方备用构建，首次可能较慢。本机无需 Docker。审核、正式发布仍需原有确认，不能将镜像构建成功理解为题目通过。

只有验证通过的题才能勾选“批量确认审核”。机器通过不等于版权/题目正确：勾选你实际检查过的题，填写审核人，再确认内容与版权。失败题可暂缓；导入失败题选择“恢复或允许重新获取”，再点“获取新题”重试。修改内容会使旧验证和审核失效。

最后可单独确认“准备发布已审核题目”，只复制已审核快照到 `knowledge/approved/`，不会自动发布。推送、版本标签、发布时再次 Docker 验证及 GitHub 发布审批仍保留。审核状态不自动赋予本机执行权限。

审核收件箱在 `knowledge/drafts/inbox/`，每题独立打包以隔离失败；队列、请求和报告在 `.trainer/authoring/`，GitHub 会话由 VS Code 管理。旧草稿不删除，原有课程、私人答案和学习进度不改变。以下手动工具已收进“高级题库维护 → 高级工具”。

## 0.8.0 批量获取开源题目（高级工具）

1. 打开用于维护题库的工作文件夹。
2. 点课程视图右上角“…” → “批量获取开源题目”，也可从“管理知识包”进入维护者工具。
3. 选择“浏览上游题目并批量导入”，扫描 C、C++ 或读取上次成功扫描的目录。
4. 选择“推荐试点”“全部入门题”或“全部扫描结果”；输入知识点搜索，勾选每批 1～50 题。
5. 输入新的草稿名称并确认。草稿保存到工作区 `knowledge/drafts/名称/`；已有文件夹不会覆盖，导入失败不会留下半成品目标包。下载缓存留在 `.imports/`，重试可复用。
6. 自动打开 `import-manifest.json`；返回维护者菜单，可查看已有清单或生成静态检查报告。

首批推荐为 32 个不同母题：24 道 C、8 道 C++，25 入门、7 进阶。上游变化后扫描数可能不同。当前提供中文标题、学习目标和知识点，完整英文题面保留；中文题干精校和初始接口补齐仍需人工审核。

本功能不创建终端，也不运行下载的代码。静态报告缺少 Docker 执行证据时 `passed=false` 是预期结果。草稿不加入课程、不授予代码信任、不提交 Git，也不自动批准或发布。离线目录仅表示可查看候选列表；未缓存过的源码仍需网络下载。

维护者菜单可打开首批 32 题的 GitHub Docker 工作流：必须先推送工具与工作流，然后在网页点击 **Run workflow**。流程重新获取固定提交的试点题并隔离验证，失败报告也会保留；这是固定试点，**不会上传或验证你任意新建的本地草稿**。其他已提交草稿使用仓库原有 `Verify draft references in Docker` 流程指定路径。无论哪种验证，后续仍需人工审核及发布审批。

网络失败可读取旧目录；缓存损坏会拒绝导入而不是绕过校验。只支持官方 Exercism C/C++ 的已识别文件布局，非试点题可能需要增加适配规则。GitHub 限流时请稍后重试，不要向插件输入密码或访问令牌。

## 第一次使用

1. 本项目用户继续打开项目根目录；新电脑也可以打开任意练习文件夹，插件已内置训练核心。
2. 点击活动栏中的 Embedded Trainer 图标。
3. 展开“C 语言基础”，再展开需要的题型。
4. 点击题目名称，在编辑区的课程面板中作答。

插件在后台调用训练核心，不会创建 PowerShell 终端。

当前有七条路线、共 198 题，包含 C 语言基础、C++ 基础、嵌入式基础、智能车竞赛、电子设计竞赛、Linux 基础和 ROS 2 基础。每条路线都具有编程题、选择题、判断题和填空题，并以入门题为主。

## Linux 与 ROS 2 基础怎么学

- 在课程树展开“Linux 基础”或“ROS 2 基础”，每条有 32 题：2 道编程、12 道选择、9 道判断、9 道填空。
- 先做入门知识题，再做配套编程，最后尝试标为“进阶”的题目；每道题都有提示与答对后的解析。
- Linux 命令知识以 Linux Bash/GNU 工具为前提，不能直接当作 PowerShell 命令使用。建议按路径 → 文件 → 权限 → 管道/重定向 → 环境与进程的顺序学习。
- ROS 2 内容以 Jazzy 入门文档为参考。建议按节点 → 话题/消息 → 服务/动作 → 参数 → 工作空间 → QoS/tf2 的顺序学习；这不是指定你必须安装 Jazzy。
- 标为“基础知识”的题目仅检查答案，不执行命令，不要求电脑安装 Linux、WSL 或 ROS 2，但插件后台仍需要 Python。
- 四道配套编程题用普通 C/C++ 模拟权限文本、路径解析、速度消息和请求响应，只依赖现有编译器。它们不测试真实 Linux 系统调用或 ROS 2 通信，不会控制硬件。
- 题干中的 ROS 2 命令假定对应包已安装、终端已加载环境；真正实践这些命令时需要另行搭建匹配环境。
- 更多学习边界与官方资料链接见项目 `content/linux-basics/README.md` 和 `content/ros2-basics/README.md`。

## 完成选择题、判断题或填空题

1. 在课程路线中点击题目。
2. 单选题选择一个选项；判断题选择“正确”或“错误”；填空题填写每一空。
3. 点击“提交答案”。
4. 回答正确后会立即显示解析，并把进度更新为“已通过”。
5. 回答错误时可以重试，也可以点击“查看提示”逐级获得线索。

客观题直接在面板完成，不会创建源码文件。填空题会自动忽略首尾空白；是否区分大小写由题目设置决定。

## 完成编程题

1. 阅读课程目标和知识点，点击“完整题目”确认要求。
2. 点击“开始练习”，插件会把初始代码复制到 `exercises/<课程 ID>/` 并打开源码。
3. 找到代码中的 `TODO`，完成实现。
4. 点击课程面板中的“运行评测”，或使用测试资源管理器的运行按钮。
5. 插件自动保存文件、编译、运行测试并更新进度。
6. 未通过时先看课程面板结果和“问题”面板，再按需查看分级提示。

第一次开始编程题后，你修改的是个人练习副本；知识包和缓存中的源文件不会被覆盖。纠错题操作相同，重点是修复提供的错误实现。代码阅读题在面板显示源码，填写输出后提交，不在学习者电脑自动运行显示的程序。

## 知识包管理

课程树顶部的包图标打开“管理知识包”，也可在命令面板搜索 `Embedded Trainer: 管理知识包`。

- **添加来源、浏览并安装**：填写你信任的 GitHub Pages `catalog.json` HTTPS 地址，再选择需要的包。源码与插件分离，新电脑不必克隆项目。项目公共清单为 [catalog.json](https://alatorrerebecca240-ship-it.github.io/WDIEmbedded-Trainer-/catalog.json)。
- **自动更新**：VS Code 运行期间默认每 24 小时检查一次；可在设置中修改检查间隔或关闭自动更新，也可随时点“检查并更新题库”。关闭 VS Code 后不会常驻后台。
- **离线导入**：选择审核后的 ZIP，粘贴可信来源给出的 64 位 SHA-256。下载错误或校验失败保留旧版，安装过程不执行包内代码。
- **回退**：选择保留版本，回退后自动暂停该包更新，避免下次检查马上升回去；管理菜单可恢复更新。
- **代码信任**：新下载的编程/纠错包首次评测前，先检查来源和源码，再选择“信任知识包代码”。这意味着允许该版本的代码在你的账号下原生运行；SHA-256 本身不能证明安全，新版本需要重新确认。客观题和代码阅读的填答不要求此授权。
- **换电脑**：每台电脑安装 VSIX，执行“检查训练环境”，Windows x64 可从配套离线包补齐缺失环境。启用 VS Code Settings Sync 的设置同步后，可信清单地址和所选包 ID 会同步，插件会下载相同订阅。它不自动同步个人答案、进度、编译器或代码信任。
- **缓存位置**：“打开版本与缓存目录”显示实际本机路径。版本目录不可手工修改，已有练习仍使用创建时缓存的版本；更新不会覆盖 `exercises/`。

未打开文件夹时使用插件私有练习目录；打开工作区时，练习代码在该工作区的 `exercises/`，进度在 `.trainer/progress.json`。本项目当前使用 `knowledge/packs/foundation.core/` 的 198 道题，旧 `content/` 留作兼容。导入和 AI/模板生成的草稿不直接出现在课程树中，必须先审核。

维护者的获取、生成、Actions Docker 审核、GitHub Releases 与 Pages 配置见项目 `docs/knowledge-pack-workflow.md`。普通学习者不需要安装 Docker 或操作发布命令。

## 课程按钮

### 开始练习 / 打开代码

仅用于编程题。第一次点击时创建练习副本，以后点击只会打开已有代码，不会重置答案。

### 运行评测

仅用于编程题，流程为：

```text
保存当前文件 → 调用编译器 → 运行自动测试 → 更新进度 → 展示结果
```

本地基础题通常提供能编译但测试失败的初始代码。原样导入的 Exercism 题可能只有空框架，缺少函数、类型或声明导致的编译错误也可能是预期起点；按题面及测试要求补全后，最终仍需编译成功并通过测试。

0.10.0 的维护验证会自动核对上游原始框架，只有原文及文件哈希对应的预期接口缺失会转为提示。参考答案必须通过完整 Docker 测试，缺依赖、超时等仍会失败。旧草稿不必重新获取，推送新版工具到 main 后点“集中审核 → 重新验证待处理题目”；已就绪/已审核且未修改的题不重复提交。云端报告区分参考答案与初始代码失败，框架提示会显示在集中审核列表，仍需人工确认后才可准备发布。

### 提交答案

用于选择题、判断题和填空题。正确后显示知识解析，错误时不会提前泄露答案。

### 查看提示

提示按信息量递增。建议从提示 1 开始，避免过早看到接近答案的信息。

### 完整题目

编程题会在编辑区打开 Markdown 说明；客观题的完整题干已经显示在课程面板中。

## 使用测试资源管理器

点击活动栏中的烧杯图标打开“测试”。Embedded Trainer 只把编程题放入这里：

- 点击题目旁的运行按钮，只评测这一题。
- 点击顶层运行按钮，依次评测全部编程题。
- 展开运行记录可查看完整编译及测试输出。

运行尚未开始的编程题时，插件会先自动创建练习副本。

## 查看学习进度

点击状态栏中的 `Trainer 已通过数/总数`，或点击课程视图顶部的进度图标。进度分为：

- **未开始**：尚未提交答案或开始编程练习。
- **进行中**：已经作答，但最近一次未通过。
- **已通过**：最近一次提交或评测通过。

进度保存在项目的 `.trainer/progress.json`，不会上传到网络。

## 常见问题

### 课程列表为空

本项目用户确认打开的是项目根目录；新电脑可打开任意练习文件夹，从面板或课程树顶部进入“管理知识包”，添加可信来源并安装题库。若仍为空，检查 Python 配置，并查看“Embedded Trainer”输出中的刷新错误。

### 新题目没有出现

点击课程视图顶部的刷新按钮。若刚更新过插件，按 `Ctrl+Shift+P` 执行“开发人员: 重新加载窗口”。

### 操作速度与刷新

0.9.1 起，提交答案和评测完成后只读取本地学习进度，不会重新扫描整个题库；重复的文件变动通知会合并，未变化的课程面板也不会重复重载。打开已有练习不需要启动 Python。题库加载、安装、更新和代码评测仍保留必要的路径、文件完整性及版本信任检查。

手动“刷新课程”会完整重载题库。题库下载、云端审核仍受网络影响，编程评测仍需要编译和运行测试；这些耗时不等同于界面刷新。若更新插件后仍像旧版一样慢，请先重新加载窗口，并在扩展详情确认版本为 0.10.1 或更新版本。

### 点击评测后提示找不到 Python

先执行“检查训练环境”，可从配套离线包补齐。已有 Python 仍无法识别时，在设置中搜索 `Embedded Trainer Python Path`，填写真实可执行文件的绝对路径。

### 提示找不到 C 或 C++ 编译器

先执行“检查训练环境”，从配套包安装缺失的 C/C++ 编译器。已配置 GCC/Clang 会优先复用；新装环境由插件直接识别，不用修改 PATH。其他系统可手动配置或使用项目提供的 Dev Container。

### 如何再次打开本说明

- 点击课程视图顶部的问号图标。
- 在课程详情面板点击“使用说明”。
- 从命令面板执行 `Embedded Trainer: 打开使用说明`。

## 内容位置

- `knowledge/packs/`：本项目的知识包源码；普通练习不直接修改。
- `content/`：保留的旧版课程源，供兼容使用。
- `exercises/`：编程题的个人练习代码。
- `.trainer/`：本机进度和临时构建结果。
- `docs/adding-a-lesson.md`：为题库作者准备的扩展说明。
