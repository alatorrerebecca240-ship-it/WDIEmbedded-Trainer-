# 工具链适配器

本目录预留给 V0.2 以后的执行适配器。统一适配器至少应实现：

```text
doctor()    检查工具链和设备
build()     构建本机程序或固件
test()      运行软件测试或硬件在环测试
flash()     可选，烧录目标板
monitor()   可选，读取串口或调试输出
```

计划中的实现包括 `native`、`cmake`、`platformio` 和厂商命令行工具。适配器输出应采用稳定的 JSON 事件，而不是让 VS Code 插件分析不同工具的自然语言输出。

