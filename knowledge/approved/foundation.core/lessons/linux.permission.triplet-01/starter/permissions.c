#include "permissions.h"

int permission_triplet(unsigned digit, char output[4])
{
    /* TODO: validate digit, then write three permission characters and '\0'. */
    (void)digit;
    (void)output;
    return -1;
}
