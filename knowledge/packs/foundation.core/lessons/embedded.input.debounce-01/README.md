# 按键消抖状态机

机械按键切换时会在 0 和 1 之间快速抖动。补全 `debounce_update`，只有同一候选状态连续出现 `threshold` 次后，才改变稳定输出。

## 要求

- 所有输入都先归一化为 0 或 1。
- 回到当前稳定状态时取消尚未完成的候选转换。
- 不使用延时、动态内存或全局变量。
- 每次调用代表一次固定周期采样。
- 本题约定 `threshold >= 1`，状态指针有效。

该题在电脑上模拟 GPIO 采样，不需要开发板。
