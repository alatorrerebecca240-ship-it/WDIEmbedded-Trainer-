# 可复用功能库

这里用于保存已经完成并经过测试的通用模块，例如环形缓冲区、滤波器、PID、协议解析器和状态机。训练题的初始代码不应直接依赖这里的参考实现，否则会泄露答案。

建议每个模块采用以下结构：

```text
module-name/
├─ include/
├─ src/
├─ tests/
├─ CMakeLists.txt
└─ README.md
```

课程通过“完成后导出”流程将合格实现复制到个人功能库；该流程将在后续版本加入。

